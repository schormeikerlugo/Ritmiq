/**
 * Skeleton — placeholders genericos con shimmer animado.
 *
 * Export principal: Skeleton (primitiva) + composiciones predefinidas.
 *
 *   import { Skeleton, TrackRowSkeleton, HeroSkeleton, GridCardSkeleton }
 *     from '../Skeleton/index.js';
 *
 *   <TrackRowSkeleton count={8} />
 *   <Skeleton variant="circle" width={36} height={36} />
 */
export {
  Skeleton,
  TrackRowSkeleton,
  HeroSkeleton,
  GridCardSkeleton,
} from './Skeleton.jsx';
